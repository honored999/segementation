# Next

## Current focus
- Execute a bounded server-side ADN diagnostic run and inspect representative case QC.

## Next actions
1. Train with the explicit Fold 0 diagnostic CLI in a new generated-output directory.
2. Generate case005 QC from the selected checkpoint and inspect the three four-panel axial views plus `summary.json`.
3. Record failures or observations without changing the fixed split, raw NIfTI files, ADN ranges, or segmentation pipeline.

## Blockers
- Real Dataset501 and GPU execution are server-only and were not validated locally.
